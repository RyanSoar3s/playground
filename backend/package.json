{
  "name": "playground-backend",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "bun run --hot ./src/app.ts",
    "build": "bun build ./src/app.ts --outdir dist"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "zod": "^4.4.3"
  }
}
